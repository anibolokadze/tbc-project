import LoginForm from "../../components/LoginForm";
import Layout from "../../components/layout";

const page = async () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default page;
